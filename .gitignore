# 构建输出
build/

# 用户词典数据库（每台机器不同）
*.userdb/
*.userdb.*
*.userdb.txt
*.userdb.kct.sync

# 安装信息（包含机器唯一 ID）
installation.yaml

# 自动生成的构建时间戳
user.yaml

# 同步文件夹
sync/

# 日志
*.log

# 语法模型（体积大，不入库；部署时由脚本复制到用户目录）
*.gram
